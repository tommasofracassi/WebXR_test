/**
 * Layer configuration for raycasting and object categorization.
 * Use Three.js Layers to filter which objects are teleportable.
 */

export const LAYERS = {
    DEFAULT: 0,
    TELEPORT: 1  // Surfaces valid for teleportation (floors)
}
